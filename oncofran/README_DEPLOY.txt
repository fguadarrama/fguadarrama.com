Deploy target: https://fguadarrama.com/oncofran/

This folder already contains the compiled static site.
Upload the complete oncofran/ folder to the root of the GitHub Pages repository.
Do not upload the previous Vite source folder as the public page unless you rebuild it first.

Expected paths after upload:
- /oncofran/index.html
- /oncofran/assets/index-*.js
- /oncofran/assets/index-*.css
- /oncofran/Portrait_nbg.png

The build uses absolute asset paths beginning with /oncofran/.
